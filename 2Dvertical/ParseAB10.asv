function [ filelist,t ] = ParseAB10(recordName)
%UNTITLED Summary of this function goes here
%   Detailed explanation goes here

	[data,metadata] = xlsread(recordName,'AB10');
	filelist = {};
	t = [];
	l = 0;
	for i = 2:length(data)
		if data(i,9) <= 6
			if data(i,29) <= 30
			if data(i,26) == 1
				l = l+1;
				filename = metadata(i,35);
				t = [t; data(i,37)];
				filelist{l} = horzcat(filename{1,1},'.smc.rm.bp.a.pad');
			end
			end
		end	
	end

				

end

