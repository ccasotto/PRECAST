
    Reactions = dlmread('nodeReaction_po.txt');
	Reactions = -sum(Reactions');
	Vb = Reactions(find(Reactions > 0));
	%VBase(1)=[];
	disp = dlmread('nodeDisp_po.txt')/(asset.ColH_upper*(asset.noStoreys-1)+asset.ColH_ground);
	u = dlmread('nodeDisp_po.txt');
	M = action.massExtNode*2+action.massIntNode*(asset.noBays-1); %mass at each floor
	Sa = (Vb./M)';
	ksm = (Sa./u);
	T = 2*pi*sqrt(1./ksm);
	%capacity curve

	